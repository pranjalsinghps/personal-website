---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="inline-flex items-center rounded-full bg-white/10 px-3 py-1.5 text-[11px] font-medium leading-none text-white/90 transition-colors hover:bg-white hover:text-black sm:text-xs">
  {name}
</span>
